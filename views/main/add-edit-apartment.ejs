<%- include('../includes/head.ejs') %>
<link rel="stylesheet" href="/css/forms.css">
<link rel="stylesheet" href="/css/apartment.css">
</head>

<body>
  <%- include('../includes/navigation.ejs') %>

  <main>
    <!-- TODO: add validation -->
    <form class="apartment-form" action="/<% if (editing) { %>edit-apartment<% } else { %>add-apartment<% } %>" method="POST" enctype="multipart/form-data">
      <div class="form-control">
        <label for="title">Title</label>
        <input class="title" type="text" name="title" id="title" value="<% if (editing) { %><%= apartment.title %><% } %>">
      </div>
      <div class="form-control">
        <label for="image">Image</label>
        <input type="file" name="image" id="image" value="<% if (editing) { %><%= apartment.imagePath %><% } %>">
      </div>
      <div class="form-control">
        <label for="price">Price</label>
        <input class="price" type="number" name="price" id="price" step="0.01" value="<% if (editing) { %><%= apartment.price %><% } %>">
      </div>
      <div class="form-control">
        <label for="description">Description</label>
        <textarea class="description" name="description" id="description" rows="5"><% if (editing) { %><%= apartment.description %><% } %></textarea>
      </div>
      <% if (editing) { %>
      <input type="hidden" value="<%= apartment.id %>" name="apartmentId" id="apartmentId">
      <% } %>

      <input type="hidden" name="_csrf" value="<%= csrfToken %>">
      <button class="btn" type="submit"><% if (editing) { %>Update Apartment<% } else { %>Add Apartment<% } %></button>
    </form>
  </main>
  <%- include('../includes/end.ejs') %>