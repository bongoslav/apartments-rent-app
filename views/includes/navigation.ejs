<nav class="navbar navbar-expand-lg bg-body-tertiary bg-dark" data-bs-theme="dark">
  <div class="container-fluid">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link <%= path === '/' ? 'active' : '' %>" aria-current="page" href="/">Apartments</a>
        </li>
        <% if (isLoggedIn) { %>
        <li class="nav-item">
          <a class="nav-link <%= path === '/favorites' ? 'active' : '' %>" aria-current="page" href="/favorites">Favorites</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= path === '/add-apartment' ? 'active' : '' %>" aria-current="page" href="/add-apartment">Add apartment</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= path === '/my-apartments' ? 'active' : '' %>" aria-current="page" href="/my-apartments">My apartments</a>
        </li>
        <% } %>
      </ul>
      <ul class="navbar-nav ms-auto">
        <% if (!isLoggedIn) { %>
        <li class="nav-item">
          <a class="nav-link <%= path === '/login' ? 'active' : '' %>" href="/login">Login</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= path === '/signup' ? 'active' : '' %>" href="/signup">Sign up</a>
        </li>
        <% } else { %>
        <li class="nav-item">
          <form action="/logout" method="post" class="d-flex">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <button type="submit" class="btn btn-outline-danger">Logout</button>
          </form>
        </li>
      </ul>
      <% } %>
    </div>
  </div>
</nav>